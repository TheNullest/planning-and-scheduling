import 'package:flutter/material.dart';

class Palette {
  static const LinearGradient blueshBackground = LinearGradient(
      colors: [Colors.black54, Color.fromRGBO(52, 1, 113, 1), Colors.black54],
      begin: Alignment.topRight,
      end: Alignment.bottomRight,
      stops: [0.1, 0.9]);
}
